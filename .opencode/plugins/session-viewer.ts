// Local dev shim: opencode auto-loads .opencode/plugins/*.ts (no npm install/build required),
// unlike the npm-package form in the repo's own opencode.json which needs a published package.
export { default } from "../../src/plugin.js";
