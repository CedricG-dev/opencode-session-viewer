---
topic: opencode-session-viewer PRD
updated: 2026-09-03T21:30
---

- (decision) Product: opencode plugin providing a browser-based multi-session dashboard (work status + cost) for opencode CLI users
- (decision) Stakes: launch tier - plugin will be published (e.g. npm) for other opencode users to install, not just personal use
- (decision) Scope: dashboard shows sessions from the current project's opencode server instance only (not cross-project aggregation across multiple opencode instances)
- (decision) Trigger: plugin auto-launches dashboard (opens browser) reactively on a relevant opencode client/bus event, not a manual slash command
- (assumption) Assumed trigger event = server.connected (opencode startup) opens the dashboard once per instance; user did not name exact event
- (assumption) Assumed content priority: must-have = session status, title, token/dollar cost, message count, last-activity time; nice-to-have = current todo/task, git diff stats, per-model cost breakdown
- (event) Fast-path draft PRD written to prd.md with 12 sections; 7 assumptions tagged, 5 open questions logged
- (change) Reviewer gate: technical fact-check against opencode source confirmed client API + in-process Bun.serve are viable; corrected FR-1 to launch directly in plugin factory body at startup instead of gating on a server.connected event (which is an SSE-handshake artifact, not a bus event); resolved Open Question 1 (trigger) and OQ3 (SessionStatus enum)
- (change) SessionStatus corrected to idle/busy/retry per real SDK types (no error status); errors sourced from session.error event instead, shown as separate signal on FR-2
- (change) Rubric review fixes: added missing FR-2 consequence for message-count/last-activity live update; fixed broken cross-ref sec5-to-sec9 -> sec2.2; added Out-of-Scope line to FR-3; deduped assumption index entry for 1s propagation latency; renumbered Open Questions after resolving two
- (event) Reviewer gate complete: rubric verdict strong/adequate across all 7 dimensions, 0 critical/high findings, 2 medium + 2 low fixed
- (decision) Session list scope: current opencode run only, not full project history across prior invocations (user decision, resolves former Open Question 2)
- (change) Fixed sec0 cross-ref bug pointing to sec9 instead of sec12 for Assumptions Index
- (event) Open-item triage complete: 2 Open Questions remain (both non-blocking, deferred with fast-follow/caveat framing), all NOTE FOR PM callouts carry revisit conditions
- (change) Polish pass (structure+prose lenses): merged sec5 Non-Goals + sec6.2 Out-of-Scope into sec5 Scope Boundaries (5.1 Never / 5.2 Deferred), resolving a never-vs-deferred inconsistency on transcript viewing and cross-project aggregation; condensed sec0 duplicate vision restatement; fixed grammar/comma-splice/e.g.-to-for-example per MS style guide; fixed 3 more cross-reference bugs (JTBD status wording, UJ-1 busy/running terminology, Project glossary FR-2 vs FR-3)
- (event) PRD finalized
